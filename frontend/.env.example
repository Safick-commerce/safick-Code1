# Copy to .env in this folder (frontend/) and fill in from Supabase Dashboard → Settings → API
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Express API (use your PC LAN IP on a physical phone, not localhost)
EXPO_PUBLIC_API_URL=http://localhost:4000
